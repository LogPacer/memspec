---
description: "Use the memspec-revisions skill to create or test debug-only genesis revision manifests for existing .memspec files."
argument-hint: "<file.memspec> [--reason \"initial import\"] [--author <name>]"
---

Use the memspec-revisions skill for this workflow and follow its safety gate exactly.

Treat any text after the prompt name as the target `.memspec` path and optional genesis metadata. Do not run a release build.
